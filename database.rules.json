{
  "rules": {
    ".read": false,
    ".write": false,
    "stats": {
      "v3": {
        "$scope": {
          ".read": true,
          ".validate": "$scope.matches(/^(comics_hujra|comics_samrqand|comics_ghailam|pages_main|pages_projects)$/)",
          "daily": {
            "$date": {
              "sessions": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "pageviews": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "qualifiedViews": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "engagedSessions": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "bounces": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "newVisitors": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "returningVisitors": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "activeMs": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 3600000"
              },
              "scrollSum": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "scrollN": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
              },
              "devices": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(mobile|desktop|tablet)$/)"
                }
              },
              "os": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
                }
              },
              "browsers": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
                }
              },
              "referrers": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
                }
              },
              "sources": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
                }
              },
              "countries": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[A-Z]{2}$/)"
                }
              },
              "hours": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^h([0-9]|1[0-9]|2[0-3])$/)"
                }
              },
              "dow": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^d[0-6]$/)"
                }
              },
              "langs": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
                }
              },
              "screens": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(xs|sm|md|lg|xl)$/)"
                }
              },
              "campaigns": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
                }
              },
              "depth": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^d(25|50|75|100)$/)"
                }
              },
              "events": {
                "$k": {
                  ".write": "newData.exists()",
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
                }
              },
              "$other": {
                ".validate": false
              },
              ".validate": "$date.matches(/^20[2-9][0-9]-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])$/)"
            }
          },
          "lifetime": {
            "sessions": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "pageviews": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "qualifiedViews": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "engagedSessions": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "bounces": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "newVisitors": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "returningVisitors": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "activeMs": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 3600000"
            },
            "scrollSum": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "scrollN": {
              ".write": "newData.exists()",
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
            },
            "devices": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(mobile|desktop|tablet)$/)"
              }
            },
            "os": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
              }
            },
            "browsers": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
              }
            },
            "referrers": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
              }
            },
            "sources": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
              }
            },
            "countries": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[A-Z]{2}$/)"
              }
            },
            "hours": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^h([0-9]|1[0-9]|2[0-3])$/)"
              }
            },
            "dow": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^d[0-6]$/)"
              }
            },
            "langs": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
              }
            },
            "screens": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(xs|sm|md|lg|xl)$/)"
              }
            },
            "campaigns": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
              }
            },
            "depth": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^d(25|50|75|100)$/)"
              }
            },
            "events": {
              "$k": {
                ".write": "newData.exists()",
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
              }
            },
            "$other": {
              ".validate": false
            }
          },
          "$other": {
            ".validate": false
          }
        }
      }
    },
    "comics": {
      "$id": {
        ".read": true,
        ".validate": "$id.matches(/^(hujra|samrqand|ghailam)$/)",
        "entries": {
          ".write": "newData.exists()",
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
        },
        "views": {
          ".write": "newData.exists()",
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
        },
        "devices": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(mobile|desktop|tablet)$/)"
          }
        },
        "os": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
          }
        },
        "browsers": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
          }
        },
        "referrers": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
          }
        },
        "hours": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^h([0-9]|1[0-9]|2[0-3])$/)"
          }
        },
        "days": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(Sun|Mon|Tue|Wed|Thu|Fri|Sat)$/)"
          }
        },
        "countries": {
          ".read": true
        }
      }
    },
    "pages": {
      "$id": {
        ".read": true,
        ".validate": "$id.matches(/^(main|projects)$/)",
        "entries": {
          ".write": "newData.exists()",
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
        },
        "views": {
          ".write": "newData.exists()",
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000"
        },
        "devices": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(mobile|desktop|tablet)$/)"
          }
        },
        "os": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
          }
        },
        "browsers": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
          }
        },
        "referrers": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^[a-z0-9_]{1,60}$/)"
          }
        },
        "hours": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^h([0-9]|1[0-9]|2[0-3])$/)"
          }
        },
        "days": {
          "$k": {
            ".write": "newData.exists()",
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() > (data.exists() ? data.val() : 0) && newData.val() - (data.exists() ? data.val() : 0) <= 1000 && $k.matches(/^(Sun|Mon|Tue|Wed|Thu|Fri|Sat)$/)"
          }
        },
        "countries": {
          ".read": true
        }
      }
    }
  }
}
